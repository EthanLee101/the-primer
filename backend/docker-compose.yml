services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: primer
      POSTGRES_PASSWORD: primer
      POSTGRES_DB: primer
    ports:
      - "5432:5432"
    volumes:
      - primer_pgdata:/var/lib/postgresql/data

volumes:
  primer_pgdata:
